name = "feelio-chat"
main = "src/index.ts"
compatibility_date = "2024-11-27"

# Non-secret config. GEMINI_API_KEY is NOT here — it's a Worker secret, set with:
#   npx wrangler secret put GEMINI_API_KEY
[vars]
FIREBASE_PROJECT_ID = "fellio-7d0b3"
# Comma-separated allowed browser origins. Add your production origin here once
# the frontend is deployed, e.g. "http://localhost:5173,https://feelio.web.app".
ALLOWED_ORIGIN = "http://localhost:5173"
